# Origin of the CredAgent FastAPI backend.
# Dev: leave unset — Vite proxies /api to http://localhost:8000 (see vite.config.js).
# Prod: set to your deployed API, e.g. https://credagent-api.onrender.com/api/v1
VITE_API_BASE=/api/v1
